variables:
	a,b:numbers,
	c:string,
	d:array of arrays of numbers

A ← 1
b ← 10
d ← [[2, 3, 5],[8,6]]

Function Kiir(a, b)
	WHILe a <= b
	      print(A)
				IF A == 1 then
					 print(" -> ")
				else_if a < B and a > 0 Then
					 print(", ") // comment
				else
					 print(" :)")
				end_if
	      a ← a + 1
	END_WHILE
end_Function

kiir(a, 10)
printLN("")
print(d[0][2])
printLN("")
